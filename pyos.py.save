import os
import curses
import pycfg
from pyarch import load_binary_into_memory
from pyarch import cpu_t

class os_t:
	def __init__ (self, cpu, memory, terminal):
		self.cpu = cpu
		self.memory = memory
		self.terminal = terminal

		self.terminal.enable_curses()

		self.console_str = ""
		self.terminal.console_print("this is the console, type the commands here\n")
		#self.terminal.console_print(str(self.terminal.get_key_buffer())) 
		self.terminal.console_print('1') 

	def printk(self, msg):
		self.terminal.kernel_print("kernel: " + msg + "\n")
		self.terminal.console_print('2') 
		
	def panic (self, msg):
		self.terminal.end()
		self.terminal.dprint("kernel panic: " + msg)
		self.cpu.cpu_alive = False
		#cpu.cpu_alive = False
		self.terminal.conso
